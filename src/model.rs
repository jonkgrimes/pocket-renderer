extern crate csv;

use std::fs::File;
use 

use geometry::Vertex3;

pub struct Model {
    verts: Vec<Vertex3<u32>>
}

impl Model {

    pub fn from_file<P: AsRef<Path>>(&self, path: P) -> Model {
        
    } 
}

